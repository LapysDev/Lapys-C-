#if preprocessed(LAPYS_PREPROCESSOR_GUARD)
  /* Guard > ... */
  #ifdef byte
  # error Lapys C++: Unexpected `byte` macro definition
  #endif

  #ifdef size_t
  # error Lapys C++: Unexpected `size_t` macro definition
  #endif

  #ifdef std
  # error Lapys C++: Unexpected `std` macro definition
  #endif

  #ifdef unreachable
  # error Lapys C++: Unexpected `unreachable` macro definition
  #endif
#endif
